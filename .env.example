# Per-site raf-bot-v2 backends (multi-tenant). One panel serves all sites; the
# site is chosen at login and pinned to the session. Add a site: create an
# API_URL_<ID> here and an entry in src/lib/sites.ts.
API_URL_DANDER=""
API_URL_TANJUNGHARJO=""

# Site used for pre-login branding before the visitor picks a location.
DEFAULT_SITE="DANDER"

NEXTAUTH_SECRET=""
NEXTAUTH_URL=""
